// ai/engine/modules/onboarding.js
const axios = require('axios');

const ONBOARDING_API = process.env.ONBOARDING_API || 'https://globalconnect.onboard/api API request failed: ' + e.message };
  }
}

module.exports = { onboardPartner };
